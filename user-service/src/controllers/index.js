import { ProfileChange } from "./slices/profile.controller.js";
import { userDetail } from "./slices/userDetail.controller.js";

export const user = {
    changeProfilePic: ProfileChange,
    userDetail
}